---
name: build
description: Build the Electron app for production (Windows installer)
---

Build the production app:

```bash
npm run build
```

Output will be in `dist/` directory.
For Windows installer: `npm run build:win`
Check `electron-builder` config in `package.json` for build targets.
