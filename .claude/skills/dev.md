---
name: dev
description: Start the Electron app in development mode
---

Run the development server:

```bash
npm run dev
```

If node_modules doesn't exist, run `npm install` first.
Watch for errors in both the main process and renderer process logs.
